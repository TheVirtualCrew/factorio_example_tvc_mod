defs = {
	prefix = "example_api_"
}

defs.settings = {
	show_table = defs.prefix .. "show_table",
	table_label = defs.prefix .. "table_label",
	min_raid_host = defs.prefix .. "min_raid_host"
}

defs.gui = {
	amount = "gui." .. defs.prefix .. "amount",
	amount_donation = "gui." .. defs.prefix .. "amount_donation",
	amount_member = "gui." .. defs.prefix .. "amount_member"
}
